# AS-027 - Engine Plugin SDK and Conformance Harness Development Log

## Feature objective

Extract a reusable dependency-light engine development contract and conformance capability from
the proven AS-026 repository boundary without creating another registry, execution path, runtime
plugin system, production engine, or artifact model.

## Repository baseline

AS-027 discovery and AS-027A documentation began from clean synchronized commit:

```text
59af99d3ab3c044bf1330d6fe2d47b664054a429
docs(engine): complete engine registry and plugin contract (AS-026G)
```

Starting branch was `main`. The approved feature branch is
`feature/AS-027-engine-plugin-sdk`, created from the same commit without an upstream. Before AS-027A
changes, `HEAD`, `origin/main`, and their merge base all matched the baseline.

## Repository discovery

The repository has one Maven project at `backend/studio-api/pom.xml` and no root aggregator POM.
`studio-api` combines the engine contract with Spring Boot/Web/Data, Hibernate, Flyway,
PostgreSQL, Playwright, Testcontainers, JUnit, and other platform concerns. The `engines/`
directory contains no Maven modules.

The complete dependency inspection covered `ExecutionEngine`, descriptor, request, result/state,
`ExecutionContext` and its snapshots, secret access/value types, preparation/workspace/source
types, workspace access/resolver, registry, orchestrator, Builtin, Playwright, and the repository
conformance interface.

## Dependency findings

- `ExecutionEngine` imports platform `ExecutionContext` and legacy lifecycle `ExecutionResult`.
- `ExecutionContext` includes runner, retry, metadata, secret-reference, suite, environment, and
  variable graphs beyond the minimum demonstrated reusable surface.
- `EngineExecutionRequest` imports platform preparation, workspace-state, source-materialization,
  and secret types.
- legacy `ExecutionResult` reaches evidence types that must not pre-empt AS-028.
- platform workspace access exposes `Path` and its request imports Jackson and preparation types.
- the current conformance interface depends on JUnit, AssertJ, and
  `ExecutionEngineRegistryImpl`.
- Builtin and Playwright canonical execution require exact request/result identity; Playwright
  additionally consumes suite configuration/reference, environment base URL, non-secret variables,
  logical secret access, and bounded prepared-source access.

Publishing `studio-api` or moving the existing closure unchanged was rejected.

## Architecture checkpoint

The approved direction separates a JDK-only production SDK from JUnit-based conformance support,
projects a least-authority engine context, retains one platform registry and orchestration path,
keeps a platform compatibility bridge, and exposes only execution-bound workspace/secret
capabilities. Exact DTO/package/workspace operations remain AS-027B validation decisions.

Plugins remain trusted statically deployed in-process code. The SDK is not a sandbox. Runtime
discovery/installation/isolation and durable artifact design remain deferred.

## AS-027A documentation implementation

Created:

- `docs/requirements/AS-027-engine-plugin-sdk-and-conformance-harness.md`;
- `docs/adr/ADR-017-engine-plugin-sdk-and-conformance-boundary.md`;
- `docs/roadmap/AS-027-implementation-plan.md`; and
- this development log.

Updated module architecture with the approved future dependency direction and reconciled current
roadmap/AS-026 completion status. No Maven module, Java source, test, build file, runtime behavior,
commit, push, PR, or merge was created by AS-027A.

## Verification

```text
git diff --check: passed
documentation path/status/terminology consistency search: passed
complete changed-document review: passed
```

Maven verification was not run because all changes are Markdown and no build/runtime file or claim
requiring execution changed.

## Independent review

Independent architecture/security review confirmed provider neutrality, one registry, one
controlled execution path, a Spring-free/JDK-only target, platform-internal persistence and
Playwright dependencies, bounded workspace authority, execution-scoped secret isolation,
deterministic layered cleanup, platform orchestration/lifecycle ownership, explicit trusted-code
assumptions, sanitized diagnostics, and no runtime-plugin or AS-028 artifact implementation.

Review corrected two documentation gaps: it named `ExecutionEnginePlugin` as the intended reusable
interface subject to AS-027B validation, and it made engine-acquired handle/value cleanup distinct
from platform-owned secret-scope and physical-workspace cleanup. No blocking finding remains.

## Repository checkpoint

AS-027A documentation implementation and independent review are complete. Changes are uncommitted
and unpushed; AS-027B remains unauthorized.
