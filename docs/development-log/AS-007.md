# AS-007 — Database Schema

> Last updated: 2026-07-14

## Objective

Create and verify the core execution database schema as a versioned Flyway migration.

## Final Status

**Completed** on `feature/AS-007-database-schema`.

**Implementation commit:** `bbcde9d feat(as-007): create core execution database schema`

## Migration

Created:

- `backend/studio-api/src/main/resources/db/migration/V2__create_execution_schema.sql`

Flyway successfully applied version 2. The verified `flyway_schema_history` entries are:

| Version | Description | Success |
|---|---|---|
| 1 | initialize database | `true` |
| 2 | create execution schema | `true` |

## Verified Tables

- `project`
- `environment`
- `test_suite`
- `execution`
- `execution_artifact`
- `flyway_schema_history`
- `schema_version_marker`

## Verified Schema Details

- The `execution` table structure, indexes, check constraints, and foreign keys were verified.
- UUID identifiers are used.
- `execution` contains a `version` column for optimistic locking.
- `execution_artifact` references `execution` with `ON DELETE CASCADE`.
- The `project`, `environment`, and `test_suite` execution relationships use `ON DELETE RESTRICT`.

## Verification

- Flyway version 2 applied successfully.
- Both recorded Flyway migrations have `success=true`.
- Maven tests passed after the migration.

## Next Recommended Story

AS-008 Execution Domain Model.
