# AS-019 - Execution Queue and Job Claiming

## Feature

Durable PostgreSQL execution queue, atomic runner claiming, renewable ownership leases,
heartbeat renewal, stale-token fencing, and expired pre-processing claim recovery.

## Branch and Starting Baseline

```text
Branch: feature/AS-019-execution-queue-and-job-claiming
Base commit: 8d061ac
Base description: Merge pull request #6 completing AS-018
Working tree at start: clean
Baseline Maven tests: 459 passing from final AS-018 verification
```

The test count is inherited baseline evidence. AS-019A changes documentation only, so the Maven
suite is not required for this phase.

## Current Phase

```text
AS-019A - Requirements and ADR
```

| Phase | Status | Review gate |
|---|---|---|
| AS-019A - Requirements and ADR | In review | Explicit approval required before AS-019B |
| AS-019B - Execution Lease Persistence | Not started | Schema and mapping approval |
| AS-019C - Atomic PostgreSQL Claiming | Not started | SQL, locking, version, and JPA-context review |
| AS-019D - Lease Heartbeat and Fencing | Not started | Security and concurrency review |
| AS-019E - Expired Lease Reclaim | Not started | Recovery-boundary and fencing review |
| AS-019F - Internal Runner Protocol | Not started | API and security review |
| AS-019G - Cancellation and Concurrency Reconciliation | Not started | Concurrency/regression review |
| AS-019H - Documentation, Regression, and Final Review | Not started | Final human approval |

AS-019B must not begin until AS-019A is completed, reviewed, and explicitly approved.

## Approved Architecture

- The existing `execution` table is the authoritative durable queue and business lifecycle record.
- `execution_lease` is the current renewable runner-ownership record.
- Claim is the acquisition operation; lease is persisted ownership.
- A `PENDING` execution initially has no lease.
- The first claim atomically creates the lease and performs `PENDING -> CLAIMED`.
- One execution has at most one lease row.
- Reclaim updates the existing lease rather than storing ownership history.
- Queue selection uses PostgreSQL `FOR UPDATE SKIP LOCKED`.
- Queue ordering is `requested_at ASC, id ASC`.
- Ownership requires runner ID plus an unpredictable claim token.
- Claim tokens fence stale ownership epochs.
- Lease generation increments on reclaim.
- PostgreSQL time controls claim, heartbeat, expiry, and reclaim.
- Heartbeats use lease-local concurrency and do not modify `Execution.version`.
- Reclaim applies only while the execution remains `CLAIMED`.
- PostgreSQL is the only durable coordination mechanism.
- Prefer a unidirectional `ExecutionLease -> Execution` persistence relationship.

## Exact Scope Boundary

AS-019 owns:

- Initial queue claiming.
- `PENDING -> CLAIMED`.
- Current renewable leases.
- Heartbeat renewal.
- Claim-token fencing.
- Reassignment of expired leases while status remains `CLAIMED`.
- Internal claim and heartbeat protocol.

Deferred beyond AS-019:

- `CLAIMED -> RUNNING`.
- Complete runner orchestration and runner registration.
- Engine execution.
- Secret resolution.
- Runtime steps, results, logs, and artifacts.
- Execution attempts and lease history.
- Retries.
- Recovery of abandoned `RUNNING` executions.
- Cooperative cancellation completion.

## AS-019A Documentation

Created:

- `docs/requirements/AS-019-execution-queue-and-job-claiming.md`
- `docs/adr/ADR-009-postgresql-execution-queue-and-renewable-runner-leases.md`
- `docs/development-log/AS-019.md`

Amended:

- `docs/adr/ADR-008-execution-management.md`
- `docs/requirements/AS-018-execution-management.md`

The amendments narrow AS-019 from a broad execution-plane story to queue and lease coordination.
Engine processing, artifacts, retries, attempts, secret resolution, and complete orchestration
remain deferred.

## Implementation Safeguards

- Do not add a second queue table.
- Do not place lease ownership fields on `execution`.
- Do not add a bidirectional lease relationship to `Execution` without documented evidence.
- Do not create more than one lease row per execution.
- Do not use the lease as attempt or history storage.
- Do not use application time for authoritative ownership.
- Do not increment `Execution.version` for heartbeat or same-status reclaim.
- Do not allow an expired or replaced token to become valid again.
- Do not use arbitrary sleeps in concurrency tests.
- During AS-019C, explicitly prove native SQL cannot leave a stale managed `Execution`.
- Do not add placeholder implementations or begin later-phase work early.

## Next Review

Review AS-019A for terminology, cross-document boundaries, lifecycle ownership, version isolation,
historical-data policy, security, deterministic tests, and phase gates. Stop before AS-019B until
explicit approval is received.
