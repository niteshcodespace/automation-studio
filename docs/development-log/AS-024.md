# AS-024 - Deterministic Playwright Java Execution Engine

## Epic Objective

Provide the first concrete browser engine using immutable execution snapshots, secure prepared
workspace access, a versioned declarative scenario, isolated headless Chromium, deterministic
cleanup, and sanitized outcomes.

## Repository Baseline

```text
Branch: main
Base commit: e814c1f
Base description: Merge pull request #13 completing AS-023
Working tree at AS-024A review start: clean
AS-023: completed, reviewed, merged, and feature branch removed
```

## Current Phase

```text
AS-024B - IMPLEMENTATION COMPLETE, PENDING REVIEW
```

| Phase | Status | Review gate |
|---|---|---|
| AS-024A - Requirements, architecture, and ADR | Completed | Approved |
| AS-024B - Engine configuration contracts | Implementation complete | Pending review |
| AS-024C - Secure versioned manifest | Not started | Blocked |
| AS-024D - Runtime boundary and Chromium adapter | Not started | Blocked |
| AS-024E - Pluggable actions and assertions | Not started | Blocked |
| AS-024F - ExecutionEngine integration | Not started | Blocked |
| AS-024G - Real-browser and security hardening | Not started | Blocked |
| AS-024H - Production readiness and documentation | Not started | Blocked |

## AS-024A Scope

AS-024A is documentation only. It adds no production source, test source, dependency, browser,
runtime configuration, schema, migration, persistence, or API behavior.

## Architecture Review Reconciliation

The initial proposal was approved with five required refinements:

1. Action dispatch is explicitly pluggable through one executor per action and an immutable,
   duplicate-safe registry. A monolithic switch/case dispatcher is prohibited.
2. Immutable execution metrics begin with the first engine implementation and cover action totals,
   action outcomes, execution duration, and browser startup duration. Persistence and reporting
   remain out of scope. Metrics use an internal runtime-result carrier and do not modify the
   provider-neutral engine result in AS-024.
3. Every raw manifest selector passes through one internal `SelectorResolver`; no page-object
   framework is introduced.
4. Manifest schema identity is an explicit contract such as `"1.0"` with deterministic future
   version negotiation and backward-compatibility rules.
5. The initial one-Browser/one-Context/one-Page runtime remains extensible for future multiple
   pages, contexts, and bounded parallel execution without implementing those capabilities now.

The following approved decisions remain unchanged:

- declarative scenario manifest;
- immutable execution snapshots;
- Chromium and headless operation only;
- one BrowserContext and one Page initially;
- no Maven, Gradle, npm, or arbitrary Java execution;
- secure repository-relative loading;
- Playwright runtime abstraction;
- deterministic cleanup; and
- the AS-024A through AS-024H story sequence.

## Documentation Added

- `docs/requirements/AS-024-playwright-execution-engine.md`
- `docs/adr/ADR-014-playwright-execution-engine.md`
- `docs/roadmap/AS-024-implementation-plan.md`
- `docs/development-log/AS-024.md`

## Next Gate

Review AS-024B before any commit or AS-024C implementation.

## AS-024B Implementation

AS-024B pins Playwright Java `1.61.0` and defines the stable `playwright-java` engine descriptor
at the same exact version without registering or implementing an execution engine. AS-022 has no
separate runtime-version dimension: the immutable snapshot, runner capability, descriptor, and
registry lookup all use this one exact version.

The strict parser consumes the existing immutable suite snapshot and accepts only Chromium,
headless operation, bounded millisecond action/navigation timeouts, bounded viewport dimensions,
an optional validated locale, and the same-origin navigation policy. Secure defaults are
deterministic. Unknown or sensitive keys, wrong JSON value types, unsupported values, and values
outside the approved bounds fail with sanitized configuration exceptions.

Operator-owned browser executable location and startup timeout are bound through a separate
immutable Spring configuration-properties record. Suite configuration cannot select the
executable or operator startup policy. The executable is optional for environment-based
provisioning; when supplied it must be an absolute syntactically valid path.

The immutable runtime metrics contract remains deferred to the AS-024D runtime boundary, where it
will be carried by the future internal runtime result. AS-024B introduces no metrics or runtime
result type.

### AS-024B Test Coverage

- exact descriptor identity, version, capability, and feature contract;
- deterministic suite configuration defaults and every explicit supported field;
- unsupported browsers and headed operation;
- wrong JSON types, timeout bounds, viewport bounds, invalid locale, unsupported policy, and
  unknown/sensitive keys; and
- operator-property defaults, explicit binding, absolute executable paths, and startup-timeout
  bounds.

At the AS-024B review gate:

```text
Focused AS-024B tests: 28 passed, 0 failures, 0 errors, 0 skipped
Full mvn test: 887 total, 880 passed, 0 failures, 0 errors, 7 skipped
Browser launch/download/runtime/scenario execution: not introduced
Engine registry integration: deferred to AS-024F
Manifest/action/selector/workspace/artifact implementation: not introduced
Persistence/Flyway/JPA/REST changes: none
Commit: not created
Push: not performed
```
