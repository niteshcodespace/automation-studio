# AS-021 - Runner Scheduling

## Feature

Runner-aware, scheduler-assisted pull that selects compatible queued executions and enforces
registered runner capacity while retaining the AS-019 execution lease as the sole ownership
model.

## Objectives

- Validate the pulling runner against AS-020 identity, lifecycle, and calculated health.
- Select the oldest compatible AS-018/AS-019 execution using immutable admission snapshots.
- Enforce `maxConcurrency` from current unexpired execution leases.
- Preserve AS-019 claiming, fencing, heartbeat, reclaim, and rollback behavior.
- Coordinate scheduling with PostgreSQL and a runner-first lock order.
- Avoid a second assignment, reservation, queue, or ownership model.

## Repository Baseline

```text
Branch: main
Base commit: 35813a6
Base description: Merge pull request #10 completing AS-020
Working tree at start: clean
AS-019: completed and merged
AS-020: completed and merged
```

The baseline provides:

- the `execution` PostgreSQL queue and immutable environment, suite, request, and test-case
  snapshots;
- atomic `PENDING -> CLAIMED` and one renewable `execution_lease` per execution;
- claim-token and generation fencing, lease heartbeat, and expired-claim reclaim;
- registered runner UUID/key identity, lifecycle, liveness, calculated health, capabilities,
  labels, and `maxConcurrency`;
- PostgreSQL `FOR UPDATE SKIP LOCKED`, database-time, and deterministic integration-test
  conventions; and
- the existing `/api/v1/runners/claim` runner pull endpoint.

AS-021A does not repeat the completed repository analysis and changes documentation only.

## Current Phase

```text
AS-021E - Atomic scheduling and lease acquisition
```

| Phase | Status | Review gate |
|---|---|---|
| AS-021A - Requirements, ADR, and development plan | Complete | Approved |
| AS-021B - Scheduling requirements persistence | Complete | Approved |
| AS-021C - PostgreSQL candidate selection | Complete | Approved |
| AS-021D - Runner eligibility and capacity | Complete | Approved |
| AS-021E - Transactional scheduling service | In review | Approve orchestration and rollback |
| AS-021F - REST integration | Not started | Approve API regression evidence |
| AS-021G - Concurrency and operational hardening | Not started | Approve hardening evidence |
| AS-021H - Final reconciliation | Not started | Final human approval |

## AS-021A Deliverables

Created:

- `docs/requirements/AS-021-runner-scheduling.md`
- `docs/adr/ADR-011-runner-aware-pull-scheduling.md`
- `docs/development-log/AS-021.md`

No Java source, Flyway migration, API, DTO, entity, repository, service, controller, mapper, or
test is changed during AS-021A.

## Architectural Decisions

### Scheduler-Assisted Pull

The runner continues to initiate `POST /api/v1/runners/claim`. The server validates the runner
and selects the oldest compatible work. There is no push dispatcher or runner inbox.

### Existing Lease Ownership

The AS-019 `execution_lease` remains the only current ownership record. A successful scheduling
decision still atomically performs `PENDING -> CLAIMED` and creates that lease. No assignment or
reservation table is introduced.

### Runner Eligibility

Scheduling requires a registered `ACTIVE`, `ONLINE` runner with a required runtime row and at
least one available slot. The existing claim `runnerId` maps to the immutable AS-020 `runnerKey`.

### Compatibility

The immutable suite snapshot's nonblank `engineId` must exactly match a key in
`runner.capabilities.engines`. Explicit admitted mandatory capabilities and label key/value pairs
also match exactly. Mutable suite state and arbitrary configuration are not inferred scheduling
requirements. Engine version negotiation is deferred.

### Lease-Derived Capacity

Capacity counts unexpired leases for the runner key whose executions are `CLAIMED`, `RUNNING`, or
`CANCEL_REQUESTED`. `lease_expires_at == databaseNow` is expired and does not consume capacity.
No available-slot counter is persisted.

### PostgreSQL Coordination

The lock order is:

```text
runner -> runner_runtime -> execution -> execution_lease
```

Same-runner requests serialize on the runner row. PostgreSQL filters compatible candidates and
uses `FOR UPDATE SKIP LOCKED` with `requested_at ASC, id ASC`. One database time controls health
and lease-expiry decisions.

### REST Boundary

The existing claim endpoint, lease duration, and response are retained. Success returns 200;
empty compatible work or exhausted capacity returns 204; malformed input returns 400; an unknown
runner returns 404; and an existing ineligible runner returns 409 through structured errors.

Request-ID idempotency is deliberately deferred. The AS-019 claim token is not a substitute.

## Risks

- Incorrect lock ordering could deadlock runner management, scheduling, and execution ownership
  operations.
- A capacity query that differs from lease expiry or lifecycle semantics could overbook or
  underutilize runners.
- JSONB compatibility predicates could become inefficient without PostgreSQL-focused query
  evidence.
- Snapshot shape drift could make older executions unschedulable; malformed or absent mandatory
  requirements must fail closed without blocking compatible later work.
- `SKIP LOCKED` may temporarily bypass the oldest compatible locked row.
- Caller-provided runner identity and capability data remain untrusted until authentication.
- A lost successful response may lead a retry to claim more work while capacity remains because
  request-ID replay protection is deferred.

## Deferred Work

- Request-ID-based idempotency.
- Authenticated runner principals and authorization.
- Runner pools, priorities, fairness, quotas, affinity, and preemption.
- Capability schemas, version negotiation, and AS-026 Engine Registry validation.
- Batch claiming and server-push dispatch.
- Persisted capacity counters or separate reservations.
- Execution attempts, ownership history, retries, and abandoned-running recovery.
- Engine execution, workspaces, secrets, steps, results, logs, and artifacts.
- Scheduling dashboards, alerting, and long-term telemetry.

## Implementation Safeguards

- Do not add a second queue, assignment, reservation, lease, or ownership token.
- Do not let the runner select an execution ID.
- Do not use mutable Automation Suite state for admitted execution compatibility.
- Do not infer scheduling constraints from free-form configuration.
- Do not use application or client time for health, lease expiry, or capacity.
- Do not persist a capacity counter in AS-021.
- Do not count expired or terminal ownership as active capacity.
- Do not reverse the runner-first lock order.
- Do not expose claim tokens or snapshots in logs, errors, or metrics.
- Do not alter AS-019 heartbeat or reclaim without a proven shared defect and explicit review.
- Do not use arbitrary sleeps in concurrency tests.
- Do not begin a later phase before the current review gate is approved.

## Review Checklist

- [ ] Requirements use AS-018 immutable snapshots, AS-019 queue/lease terminology, and AS-020
  runner identity/health/capability terminology consistently.
- [ ] Scheduler-assisted pull and the existing claim route are explicit.
- [ ] The execution lease is the sole ownership and capacity-reservation record.
- [ ] Execution and runner eligibility are complete and fail closed.
- [ ] Compatibility rules are exact and do not invent version negotiation or inferred policy.
- [ ] Capacity states and lease-expiry equality semantics are explicit.
- [ ] PostgreSQL time and `runner -> runner_runtime -> execution -> execution_lease` locking are
  explicit.
- [ ] FIFO selection among compatible executions and `SKIP LOCKED` behavior are explicit.
- [ ] REST and structured failure semantics are unambiguous.
- [ ] Security and observability prohibit token and snapshot disclosure.
- [ ] Request-ID idempotency and all other deferred work remain out of scope.
- [x] AS-021A contains documentation only.
- [x] AS-021B contains persistence and migration-test changes only.

## AS-021B Implementation

### Persistence Design

The existing execution snapshots are sufficient for the approved persistence boundary:

- `suite_snapshot.engineId` is the immutable engine requirement used by later compatibility
  selection.
- `request_snapshot` is the future source of explicitly admitted mandatory constraints.
- `environment_snapshot` remains admission-time execution context and is not duplicated.

V13 adds no column, scheduling table, assignment table, history table, or mutable scheduling
metadata. It enforces the existing snapshot immutability contract with a PostgreSQL trigger and
adds a partial expression index:

```text
(suite_snapshot ->> 'engineId', requested_at, id)
WHERE status = 'PENDING'
  AND engineId is nonblank
```

The index supports the approved exact-engine compatible FIFO query shape without implementing
that query in AS-021B. Historical null or incomplete snapshots remain valid and unchanged; later
scheduling fails them closed.

### JPA and Repository Reconciliation

No JPA model change is required. `Execution` already maps the three JSONB snapshots and returns
defensive copies. No repository change is required because scheduling queries belong to AS-021C.
The full regression proves existing Hibernate lifecycle updates remain compatible with the
database trigger when snapshot values are unchanged.

### Migration and Test Evidence

`SchedulingRequirementsMigrationIntegrationTest` covers:

- a clean migration through V13;
- trigger and partial expression-index catalog definitions;
- rejection of changes to environment, suite, and request snapshots;
- continued acceptance of ordinary execution lifecycle updates;
- upgrade from a populated V12 schema;
- byte-for-byte logical preservation of historical snapshots; and
- immutability enforcement after upgrade.

Verification:

```text
Focused AS-021B migration tests:
Tests run: 3, Failures: 0, Errors: 0, Skipped: 0

AS-018 through AS-021 migration regression:
Tests run: 13, Failures: 0, Errors: 0, Skipped: 0

Full mvn clean test:
Tests run: 586, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

Existing compiler notices for deprecated test APIs, unchecked test operations, and dynamic
Mockito agent loading remain unchanged.

### AS-021B Scope Confirmation

AS-021B adds no scheduling service, compatibility matcher, capacity calculation, runner
selection, claim workflow change, controller/API change, concurrency orchestration, or scheduler
algorithm.

## AS-021C Implementation

### Scheduling Domain

AS-021C adds immutable scheduling values:

- `SchedulingRequirements` for the required engine, capability JSON, and label pairs;
- `RunnerCapabilities` for the runner's deeply copied advertised capability and label documents;
- `CompatibilityResult` with engine, capability, and label mismatch outcomes;
- `RunnerCompatibilityEvaluator` for exact engine/label matching and recursive JSON containment;
  and
- `SchedulingCandidate` for a read-only execution candidate projection.

`SchedulingRequirementsParser` validates the complete AS-018 environment, suite, and request
snapshot shapes. Snapshot environment/suite IDs, selection mode, requester, and request timestamp
must match their relational execution fields. Missing, malformed, or inconsistent snapshots fail
closed and are never repaired.

Optional admitted constraints are read only from:

```text
request_snapshot.requiredCapabilities
request_snapshot.requiredLabels
```

Absence means no additional constraint. A present capability value must be an object; required
labels must be a string-to-string object. `suite_snapshot.engineId` remains mandatory.

### Candidate Repository

`SchedulingCandidateRepository` and `PostgreSqlSchedulingCandidateRepository` provide one
read-only operation that returns at most the oldest compatible candidate.

PostgreSQL:

- restricts status to `PENDING`;
- excludes any execution already carrying an `execution_lease`;
- validates required snapshot structure and relational consistency;
- matches the engine against the runner's registered engine keys;
- applies JSONB containment for mandatory capabilities and labels;
- fails closed for historical incomplete snapshots;
- orders by `requested_at ASC, id ASC`; and
- can use the V13 `idx_execution_pending_engine_queue` or the existing
  `idx_execution_pending_queue` for indexed compatible FIFO access, according to PostgreSQL
  planner costs; and

The query contains no `FOR UPDATE` or `SKIP LOCKED`. It creates no lease and changes no execution.
Lock acquisition and transactional ownership remain assigned to later phases.

### AS-021C Tests

Domain tests cover compatible and incompatible engines, missing/malformed engine requirements,
malformed/inconsistent snapshots, recursive capability matching and mismatch, label matching and
mismatch, and explicit compatibility results.

PostgreSQL Testcontainers coverage verifies:

- requested-time FIFO ordering;
- UUID tie-break ordering;
- `PENDING` and no-existing-lease eligibility;
- exclusion of every non-pending status;
- engine, capability, and label compatibility;
- historical incomplete snapshot fail-closed behavior without blocking a later valid candidate;
- absence of lifecycle or lease mutation; and
- indexed pending FIFO access with an engine-filtered plan, while the migration test independently
  verifies the exact V13 engine queue index definition.

Focused AS-021C verification passed 13 tests. The relevant AS-019 through AS-021 regression set
passed 29 tests. The full `mvn clean test` regression passed 599 tests with no failures, errors,
or skips.

### AS-021C Scope Confirmation

AS-021C adds no runner eligibility or health decision, capacity calculation, maximum-concurrency
enforcement, lease acquisition, claim token, scheduler transaction, claim workflow change, REST
change, assignment history, background process, or push scheduling.

## AS-021D Implementation

### Advisory Eligibility Evaluation

AS-021D adds a read-only runner scheduling evaluation service and immutable domain results. The
service resolves the immutable runner key, reads one PostgreSQL `clock_timestamp()` value, loads
the registered runner and runtime, counts capacity-consuming leases at that same instant, and
returns explicit eligibility failures without modifying or repairing persistence.

Eligibility requires:

- an existing runner in `ACTIVE` lifecycle status;
- a matching, present runtime row whose AS-020 health is `ONLINE`;
- positive `maxConcurrency`;
- structurally valid registered capabilities and string-to-string labels; and
- at least one available capacity slot.

Missing runners, invalid lifecycle, missing or mismatched runtime identity, stale/offline
heartbeats, invalid concurrency, malformed capability/label documents, and exhausted capacity
remain distinguishable evaluation outcomes. This phase does not expose those outcomes through
REST.

### Health and Capacity Semantics

Health reuses the AS-020 thresholds exactly: age at or below the online threshold is `ONLINE`;
age above online and at or below offline is `STALE`; and age above offline is `OFFLINE`. A future
heartbeat has zero effective age. The same PostgreSQL evaluation timestamp drives both health
and lease expiry.

Capacity-consuming ownership is an execution lease for the runner whose execution is `CLAIMED`,
`RUNNING`, or `CANCEL_REQUESTED` and whose `lease_expires_at` is strictly greater than the
evaluation timestamp. Equality is expired and does not consume capacity. Capacity is derived
without a persisted counter:

```text
availableCapacity = max(0, maxConcurrency - activeLeaseCount)
```

The result preserves the observed active count while preventing negative available capacity if
legacy or concurrent data already exceeds the configured maximum.

### Transaction and Locking Boundary

Evaluation runs in a read-only transaction and acquires no pessimistic locks. It is advisory:
AS-021D does not reserve a slot, select or lock an execution, create a lease, or guarantee that
capacity remains available after the result is returned. AS-021E will perform the authoritative
atomic scheduling transaction using the approved
`runner -> runner_runtime -> execution -> execution_lease` lock order.

### AS-021D Tests

Domain tests cover eligible runners; lifecycle, runtime, health, concurrency, capacity, and
metadata failures; exact health thresholds; future heartbeats; capacity clamping; and the
explicit missing-runner result.

PostgreSQL Testcontainers coverage verifies:

- only unexpired leases in ownership-bearing execution statuses consume capacity;
- expired, terminal, and other-runner leases are excluded;
- `lease_expires_at == evaluatedAt` is expired;
- full capacity is reported exactly;
- the evaluation timestamp comes from PostgreSQL;
- missing runtime rows are not repaired; and
- evaluation changes no runner, runtime, execution, or lease state.

Focused AS-021D verification passed 13 tests with no failures, errors, or skips.
The relevant AS-019 through AS-021 regression set passed 60 tests. The full
`mvn clean test` regression passed 612 tests with no failures, errors, or skips.

The AS-021C plan assertion was reconciled after PostgreSQL correctly chose either the V13
engine-aware index or the existing pending FIFO index according to table statistics. Its
representative fixture now supplies explicit statistics without planner overrides, requires an
engine-filtered indexed plan using either applicable pending index, and leaves exact V13 index
shape verification to the migration test. Functional engine filtering and FIFO assertions remain
unchanged.

### AS-021D Scope Confirmation

AS-021D adds no claim or lease acquisition, candidate orchestration, pessimistic locking,
capacity reservation, persisted counter, controller/API change, assignment model, background
process, or push scheduling.

## AS-021E Implementation

### Authoritative Transaction

`RunnerSchedulingService` exposes one internal scheduling attempt using a canonical runner key and
the existing AS-019 lease duration. Caller-supplied capabilities, labels, health, capacity, and
execution identity are not accepted. One transaction:

1. locks the registered runner by immutable key;
2. locks its runtime row;
3. reads one PostgreSQL `clock_timestamp()`;
4. re-evaluates lifecycle, runtime identity, health, metadata structure, and lease-derived
   capacity;
5. selects and locks the oldest compatible execution;
6. revalidates `PENDING`, absence of an existing lease, and immutable-snapshot compatibility;
7. applies the existing `PENDING -> CLAIMED` domain transition; and
8. creates and flushes the AS-019 execution lease.

The explicit lock order is:

```text
runner -> runner_runtime -> execution -> execution_lease
```

Capacity counting reads leases but takes no lease row lock. The execution is locked before its
state changes, and the execution-lease insert is the final ownership operation.

### Time, Capacity, and Ownership

The single database timestamp controls runner health, active-lease expiry, `claimedAt`,
`lastHeartbeatAt`, and the new lease expiry. Active capacity retains AS-021D semantics:
`CLAIMED`, `RUNNING`, or `CANCEL_REQUESTED` with `lease_expires_at > evaluatedAt`.

Same-runner attempts serialize on the runner row, so every attempt recounts capacity after the
previous transaction commits. No runner load counter or reservation is persisted. The execution
lease remains the sole assignment and ownership record.

### Candidate Locking and No-Work Semantics

The AS-021C compatible FIFO query now also has a locking operation using:

```text
ORDER BY requested_at ASC, id ASC
FOR UPDATE OF execution SKIP LOCKED
LIMIT 1
```

Locked candidates may be skipped so independent runners can make progress; FIFO is deterministic
when uncontended and best-effort under contention. The bounded one-row selection prevents
duplicate ownership without locking an unbounded candidate set. Malformed and incompatible
snapshots still fail closed and remain unchanged.

`SchedulingResult` distinguishes successful scheduling, missing runner, ineligible runner,
capacity exhaustion, and no compatible work. Expected no-work outcomes create no lease and
change no execution.

### Atomicity and Test Evidence

Focused unit tests cover input/result invariants, missing and stale runners, exact capacity
exhaustion, no-work behavior, successful transition, and lease construction.

PostgreSQL Testcontainers tests cover compatible FIFO scheduling, engine/capability/label
filtering, malformed snapshots, expired and active capacity, no-work immutability, same-runner
serialization at one and multiple slots, different-runner contention for one candidate, lease
timestamps, and injected lease-insert failure. The injected failure proves the preceding
execution transition rolls back to `PENDING` with no lease.

Focused AS-021E verification passed 13 tests with no failures, errors, or skips.
The full `mvn clean test` regression passed 625 tests with no failures, errors, or skips.

### AS-021E Scope Confirmation

AS-021E adds no controller, public API or DTO change, request-ID idempotency, retry loop,
background scheduler, push dispatch, assignment table, ownership history, capacity counter,
lease renewal/reclaim change, or runner registration change.

## Next Implementation Phases

### AS-021F - REST Integration

Wire the existing claim endpoint to runner-aware scheduling while preserving response and
structured error contracts.

### AS-021G - Concurrency and Operational Hardening

Prove capacity limits, duplicate prevention, lock ordering, compatibility under contention, and
token-safe operational signals.

### AS-021H - Final Reconciliation

Reconcile requirements, ADR, implementation, tests, and development log; run the full regression
and repository hygiene verification.

## Review Gate

AS-021E is implemented and remains uncommitted pending review. Stop here until explicit approval
to begin AS-021F.
